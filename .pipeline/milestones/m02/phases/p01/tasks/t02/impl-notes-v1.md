- `packages/ui/src/components/textarea/textarea.types.ts`: created `TextareaProps` type extending `React.ComponentProps<'textarea'>` and `VariantProps<typeof textareaVariants>`, with `asChild?: boolean` and `autoResize?: boolean`.
- `packages/ui/src/components/textarea/textarea.styles.ts`: created `textareaVariants` CVA base class definition for textarea layout, focus, disabled, placeholder, and `aria-invalid` error styling.
- `packages/ui/src/components/textarea/textarea.tsx`: created `Textarea` component with Radix `Slot` `asChild` support, `data-slot="textarea"`, `cn(textareaVariants({ className }))`, React 19 `ref` prop usage, and conditional `style` merge applying `fieldSizing: 'content'` when `autoResize` is true.
- `packages/ui/src/components/textarea/textarea.test.tsx`: created full Vitest suite (15 tests) covering render, `data-slot`, class merge, disabled/error/placeholder behavior, controlled/uncontrolled modes, `autoResize` style behavior, style merge, `asChild`, ref forwarding, and two `vitest-axe` accessibility checks.
- `packages/ui/src/components/textarea/textarea.stories.tsx`: created CSF3 Storybook stories with `tags: ['autodocs']`, controls, and stories for `Default`, `WithPlaceholder`, `WithValue`, `Disabled`, `WithError`, `AutoResize`, and `WithLabel`.
- `packages/ui/src/index.ts`: added public exports for `Textarea`, `TextareaProps`, and `textareaVariants`.
