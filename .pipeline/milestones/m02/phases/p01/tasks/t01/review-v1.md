{"verdict":"needs_revision","feedback":"Input task deliverables themselves match the locked plan/spec (all 5 files exist, exports are correct, targeted Input tests pass, `@components/ui` typecheck/lint pass, and `pnpm build` succeeds). But required full test runs (`pnpm --filter @components/ui test` and root `pnpm test`) fail due code-level test failures, so the exit criterion requiring all tests to pass is not met.","issues":[{"file":"packages/ui/src/components/collapsible/collapsible.test.tsx","severity":"major","description":"Full test runs fail on `Collapsible > keyboard activation (Enter/Space)` (`expect(content).not.toBeVisible()`), indicating a code-level failing test (not an environment-only failure)."},{"file":"packages/ui/src/components/sonner/sonner.test.tsx","severity":"major","description":"Full test runs fail on three Sonner tests (`renders Toaster without crashing`, `data-slot on toaster container`, `uses dark theme...`) because `[data-slot=\"sonner\"]` resolves to null, producing assertion failures."}]}
