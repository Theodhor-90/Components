{"verdict":"needs_revision","feedback":"The plan exports `selectScrollButtonVariants` from `packages/ui/src/index.ts`, but `SelectScrollUpButton` and `SelectScrollDownButton` are explicitly internal components (not exported as public API per DD-5 and the task spec). Exporting a CVA variant function for internal-only sub-components is unnecessary public API surface — consumers have no use for it. Remove `selectScrollButtonVariants` from the `index.ts` export block.","issues":[{"category":"scope","description":"Section 3.6 (index.ts Modifications) exports `selectScrollButtonVariants` from `packages/ui/src/index.ts`, but SelectScrollUpButton and SelectScrollDownButton are explicitly internal to SelectContent and not publicly exported as components. Exporting their CVA variant function adds unnecessary public API surface that no consumer can use. Remove `selectScrollButtonVariants` from the styles export block in index.ts."}]}
