{"verdict":"needs_revision","feedback":"The draft is thorough and well-structured overall but has several issues: scope creep via SelectValue (not in parent spec), inconsistent style patterns between DD-4 claims and actual task specs, ambiguity in animations/positioning, and a few minor correctness/completeness gaps.","issues":[{"category":"scope","description":"DD-6 introduces `SelectValue` as a publicly exported sub-component. The milestone spec (Phase 2 section) explicitly enumerates: SelectTrigger, SelectContent, SelectItem, SelectGroup, SelectLabel, SelectSeparator. `SelectValue` is not listed. The phase seed spec also does not mention it. Exporting it as a public API addition is scope creep. If it's needed internally by SelectTrigger to render the placeholder/value, it can remain an internal implementation detail — but it should not appear as a standalone public export in `select.types.ts`, `select.tsx`, or the `index.ts` export block."},{"category":"ambiguity","description":"DD-4 states that components without configurable variants use 'CVA with base classes only' (Checkbox, Switch, Radio Group), while compound components with no variants use 'plain exported string constants' (Select). However, the rationale for why Checkbox/Switch/Radio Group deserve CVA wrappers while Select uses plain strings is not clearly justified — the only stated criterion is 'compound vs single', but Toggle Group is compound AND uses CVA. Clarify: the decision should be based on whether the style export needs to participate in `VariantProps` typing. For Checkbox/Switch/RadioGroup/RadioGroupItem, there are no variants, so plain strings would be equally valid and simpler. Either standardize on one approach for no-variant components, or explicitly justify why two different patterns coexist."},{"category":"ambiguity","description":"Select `selectContentStyles` includes Tailwind CSS animation classes (`animate-in`, `animate-out`, `fade-in-0`, `fade-out-0`, `zoom-in-95`, `zoom-out-95`, `slide-in-from-top-2`, etc.). These are NOT default Tailwind v4 utilities — they come from `tailwindcss-animate` plugin. The draft does not mention this dependency or confirm it is already installed. If it's not installed, these classes will silently produce no effect. The draft must either confirm this plugin is already available or explicitly add it as a dependency."},{"category":"ambiguity","description":"Task 6 (Select) mentions `selectContentStyles` includes 'plus popper-specific sizing classes applied conditionally' in parentheses but does not specify what those classes are or how the conditional logic works (runtime className concatenation? separate style string?). The implementer would have to guess or consult shadcn/ui source. Spell out the popper-specific classes and the conditional application mechanism."},{"category":"correctness","description":"DD-1 claims 'Radix Checkbox, Switch, Toggle, and Select do not expose an asChild prop on their root in a meaningful way.' This is incorrect — Radix primitives DO support `asChild` on their Root components (it's part of the base Radix primitive API). The decision not to add custom `asChild` handling is fine, but the justification is factually wrong. Rewrite the rationale: these components don't need custom `asChild` because the Radix props spread already includes `asChild` from the primitive type, so it's automatically available to consumers without any special handling."},{"category":"completeness","description":"The draft specifies installing 6 Radix dependencies as separate tasks spread across Tasks 1-6, but does not specify a single coordinated installation step. For practical implementation, it would be cleaner to install all 6 Radix packages in Task 1 (or as a preliminary step) rather than installing one per task. This avoids 6 separate `pnpm install` cycles and potential lockfile churn. Either add a preliminary dependency installation task or explicitly state in Task 1 that all 6 packages should be installed upfront."}]}
