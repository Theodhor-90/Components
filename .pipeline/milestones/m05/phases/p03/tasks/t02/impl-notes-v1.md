- **`packages/ui/src/components/combobox/combobox.types.ts`** — Replaced flat `ComboboxProps` with discriminated union (`ComboboxSingleProps | ComboboxMultipleProps`). Added `mode` prop (`'single' | 'multiple'`), `onCreateOption` callback. Single mode uses `string` value types, multiple mode uses `string[]`.
- **`packages/ui/src/components/combobox/combobox.tsx`** — Added `useCallback` import. Internally normalizes values to `string[]` via `useControllableState`. Added `searchValue` state for controlled `CommandInput`. Added `handleSelect` with mode-aware toggle (multi keeps popover open) vs set-and-close (single). Added mode-aware trigger display (`"{n} selected"` for multi >1). Added conditional create-option `CommandGroup`/`CommandItem` when `onCreateOption` provided, search non-empty, and no label match. Added custom `filter` prop on `Command` to always show `__create__` item.
- **`packages/ui/src/components/combobox/combobox.test.tsx`** — Added `describe('Combobox multi-select')` block with 6 tests: toggles items, trigger single label, trigger count, popover stays open, onValueChange fires with array, accessibility. Added `describe('Combobox create-option')` block with 5 tests: appears for unmatched input, callback fires, hidden when match, not shown without callback, clears search after creation, multi-select with create-option. All 12 original tests unchanged and passing.
- **`packages/ui/src/components/combobox/combobox.stories.tsx`** — Added `WithCreateOptionCombobox` and `MultiSelectWithCreateOptionCombobox` wrapper components with `useState` for dynamic options. Added 4 stories: `MultiSelect`, `MultiSelectWithDefaults`, `WithCreateOption`, `MultiSelectWithCreateOption`.