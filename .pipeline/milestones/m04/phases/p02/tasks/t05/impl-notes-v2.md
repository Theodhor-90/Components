- [pnpm-lock.yaml](/Users/theodhor/Desktop/Portfolio/Components/pnpm-lock.yaml): synced lockfile with `packages/ui/package.json` by adding `@radix-ui/react-progress` to the `packages/ui` importer and adding the corresponding `packages` + `snapshots` entries for `@radix-ui/react-progress@1.1.7` (including integrity, peers, and transitive deps); `pnpm install --frozen-lockfile --lockfile-only` now succeeds.
- Verification: `pnpm typecheck`, `pnpm --filter @components/ui test -- progress`, `pnpm test`, `pnpm --filter @components/ui build`, and `pnpm storybook` are blocked in this environment because dependency installation cannot complete (`ENOTFOUND registry.npmjs.org`), leaving `turbo`/`vitest`/`tsc` unavailable.
