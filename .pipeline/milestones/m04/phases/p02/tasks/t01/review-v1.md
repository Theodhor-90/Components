{"verdict":"needs_revision","feedback":"Avatar implementation files and exports align with the locked plan/spec, but the dependency integration is incomplete: the lockfile was not updated for `@radix-ui/react-avatar`. This is a CI-blocking reproducibility defect. Informationally, additional command failures were environment-related after install attempts in this sandbox (no registry access and missing `turbo`), so full test/build execution could not be completed here.","issues":[{"file":"pnpm-lock.yaml","severity":"critical","description":"`packages/ui/package.json` adds `@radix-ui/react-avatar` but `pnpm-lock.yaml` has no corresponding specifier/importer/package entries (e.g., no `@radix-ui/react-avatar` entry under `packages/ui`). `pnpm install --frozen-lockfile --offline` fails with `ERR_PNPM_OUTDATED_LOCKFILE`, confirming lockfile/package.json mismatch."}]}
