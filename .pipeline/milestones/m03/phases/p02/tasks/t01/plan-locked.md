Now I have all the context needed. Let me produce the comprehensive implementation plan.

# Task 1: Install `react-resizable-panels` dependency — Implementation Plan

## 1. Deliverables

| File                       | Action        | Purpose                                                   |
| -------------------------- | ------------- | --------------------------------------------------------- |
| `packages/ui/package.json` | Modify        | Add `react-resizable-panels` to the `dependencies` object |
| `pnpm-lock.yaml`           | Auto-modified | Updated by `pnpm install` to resolve the new dependency   |

## 2. Dependencies

- **`react-resizable-panels`** — The third-party library that the Resizable component (Task 4 of this phase) wraps. Must be added as a runtime `dependency` (not `devDependency` or `peerDependency`) because consumer apps need it at runtime.
- **No other packages** are required for this task. All other dependencies needed by Phase 2 (`@radix-ui/react-slot` for Breadcrumb/Sidebar `asChild`) are already installed.

## 3. Implementation Details

### `packages/ui/package.json`

**Purpose:** Declare `react-resizable-panels` as a runtime dependency of `@components/ui`.

**Change:** Add one line to the `dependencies` object:

```json
"react-resizable-panels": "^2.1.7"
```

**Placement:** Insert alphabetically within the `dependencies` block, between `"react-hook-form"` and `"sonner"`.

**Version selection:** Use the latest stable v2.x release (^2.1.7 or whichever the latest is at install time). The `^` prefix allows minor/patch upgrades. The shadcn/ui reference uses `react-resizable-panels` v2.x. Do NOT use v1.x or v3.x (v3 does not exist yet).

**Full `dependencies` section after the change:**

```json
"dependencies": {
  "@components/utils": "workspace:*",
  "@hookform/resolvers": "^5.2.2",
  "@radix-ui/react-accordion": "^1.2.12",
  "@radix-ui/react-alert-dialog": "^1.1.15",
  "@radix-ui/react-checkbox": "^1.3.2",
  "@radix-ui/react-collapsible": "^1.1.12",
  "@radix-ui/react-dialog": "^1.1.15",
  "@radix-ui/react-label": "^2.1.8",
  "@radix-ui/react-popover": "^1.1.15",
  "@radix-ui/react-radio-group": "^1.3.2",
  "@radix-ui/react-scroll-area": "^1.2.10",
  "@radix-ui/react-select": "^2.2.4",
  "@radix-ui/react-separator": "^1.1.8",
  "@radix-ui/react-slider": "^1.3.6",
  "@radix-ui/react-slot": "^1.2.4",
  "@radix-ui/react-switch": "^1.2.2",
  "@radix-ui/react-tabs": "^1.1.13",
  "@radix-ui/react-toggle": "^1.1.9",
  "@radix-ui/react-toggle-group": "^1.1.10",
  "@radix-ui/react-visually-hidden": "^1.2.4",
  "class-variance-authority": "^0.7.1",
  "react-hook-form": "^7.71.2",
  "react-resizable-panels": "^2.1.7",
  "sonner": "^2.0.7",
  "tailwindcss-animate": "^1.0.7",
  "zod": "^4.3.6"
}
```

### `pnpm-lock.yaml`

**Purpose:** Lock the resolved version of `react-resizable-panels` and its transitive dependencies.

**Change:** Automatically generated by running `pnpm install`. No manual editing required.

## 4. API Contracts

N/A — This task installs a dependency. No API surface is created or modified.

## 5. Test Plan

This task has no test files to create. Verification is done via commands that confirm:

1. **Package resolution:** `react-resizable-panels` resolves correctly in the `@components/ui` workspace.
2. **No regressions:** Existing tests and type checks continue to pass.

### Test 1: Package is listed in dependencies

- **What:** Confirm `react-resizable-panels` appears in `packages/ui/package.json` under `dependencies`.
- **How:** Read the file or run `pnpm ls react-resizable-panels --filter @components/ui`.
- **Expected:** The package is listed with a version matching `^2.x`.

### Test 2: TypeScript type check passes

- **What:** Ensure the new dependency does not introduce type conflicts.
- **How:** Run `pnpm typecheck`.
- **Expected:** Zero errors.

### Test 3: Existing tests pass

- **What:** Ensure no regression from the dependency addition.
- **How:** Run `pnpm test`.
- **Expected:** All existing tests pass.

## 6. Implementation Order

1. **Add `react-resizable-panels` to `packages/ui/package.json`** — Insert the dependency entry in the `dependencies` object, alphabetically between `"react-hook-form"` and `"sonner"`.
2. **Run `pnpm install`** — This resolves the package, downloads it, and updates `pnpm-lock.yaml`.
3. **Verify installation** — Run `pnpm ls react-resizable-panels --filter @components/ui` to confirm the package is resolved.
4. **Run `pnpm typecheck`** — Confirm no type regressions.
5. **Run `pnpm test`** — Confirm no test regressions.

## 7. Verification Commands

```bash
# Step 1: Install after editing package.json
pnpm install

# Step 2: Verify the package is installed in the correct workspace
pnpm ls react-resizable-panels --filter @components/ui

# Step 3: Verify no type regressions
pnpm typecheck

# Step 4: Verify no test regressions
pnpm test
```

## 8. Design Deviations

None.
