All verifications pass:

- `react-resizable-panels@2.1.9` resolved in `@components/ui`
- `pnpm typecheck` — 0 errors
- `pnpm test` — 28 test files, 396 tests passed

---

- **`packages/ui/package.json`** — Added `"react-resizable-panels": "^2.1.7"` to `dependencies` (resolved to 2.1.9)
- **`pnpm-lock.yaml`** — Auto-updated with `react-resizable-panels` resolution
