- [spinner.tsx](/Users/theodhor/Desktop/Portfolio/Components/packages/ui/src/components/spinner/spinner.tsx): Updated Spinner markup to include a default `<span className="sr-only">Loading</span>`, omit that span when `aria-label` is explicitly provided, and keep the SVG arc rendering/ref forwarding under a `role="status"` root with `data-slot="spinner"`.
- [spinner.test.tsx](/Users/theodhor/Desktop/Portfolio/Components/packages/ui/src/components/spinner/spinner.test.tsx): Reworked assertions to cover required `sr-only` label behavior (default presence + omission with `aria-label`) and adjusted size/class checks to target the inner `<svg>` while retaining coverage for data-slot, stroke/stroke-dasharray, ref forwarding, and axe accessibility.
