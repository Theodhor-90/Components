- [packages/ui/src/components/dialog/dialog.test.tsx](/Users/theodhor/Desktop/Portfolio/Components/packages/ui/src/components/dialog/dialog.test.tsx): added missing focus-trap test (`Tab` stays within dialog), added ARIA linkage test (`aria-labelledby`/`aria-describedby` map to title/description IDs), expanded custom `className` coverage across `DialogTrigger`, `DialogOverlay`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`, and `DialogClose`, and hardened overlay-click test with explicit element narrowing.
- Verification: ran `pnpm install`, `pnpm --filter @components/ui test -- --grep "Dialog"`, `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`; install failed with `ENOTFOUND registry.npmjs.org`, and test/build/typecheck/lint failed due missing local binaries (`vitest`/`turbo`) from incomplete install.
