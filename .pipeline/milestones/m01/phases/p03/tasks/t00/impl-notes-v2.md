- [pnpm-lock.yaml](/Users/theodhor/Desktop/Portfolio/Components/pnpm-lock.yaml): added missing `packages/ui` importer specifiers/versions for `@radix-ui/react-collapsible`, `@radix-ui/react-label`, and `@radix-ui/react-visually-hidden`; added corresponding `packages` entries (integrity, peers) and `snapshots` entries for resolved versions `1.1.12`, `2.1.7`, and `1.2.3`.
- Verification: `pnpm install --frozen-lockfile` now reports `Lockfile is up to date` (then fails fetch with `ENOTFOUND registry.npmjs.org`), `node -e` dependency presence check passes, `pnpm typecheck` and `pnpm test` fail in this environment with `turbo: command not found`.
